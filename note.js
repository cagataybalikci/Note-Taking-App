module.exports = class Note{
    constructor(noteTitle,noteDescription,noteDate,){
        this.noteTitle = noteTitle;
        this.noteDate = noteDate;
        this.noteDescription = noteDescription;
    }
}

